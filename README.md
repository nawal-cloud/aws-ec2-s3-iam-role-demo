# aws-ec2-s3-iam-role-demo
his project demonstrates how to configure an **IAM role** that allows an **EC2 instance** to securely access an **S3 bucket** without using access keys.   Hands-on was performed via the **AWS Management Console**, with CLI verification.
